const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),vm=require("node:vm");const root=path.resolve(__dirname,"..");const view=fs.readFileSync(path.join(root,"Views/Report/_C7DailyChargeDetailReport.cshtml"),"utf8"),script=fs.readFileSync(path.join(root,"wwwroot/js/reports/c7-report.js"),"utf8"),app=fs.readFileSync(path.join(root,"wwwroot/js/report-app.js"),"utf8");for(const x of ["page-title","panel query-panel","date-row","panel result-panel","empty-result","pagination","_TableSkeleton","type=\"date\""])assert.match(view,new RegExp(x));assert.match(view,/<select v-model="form\.inputUserId" required/);assert.match(view,/<option value="">請選擇輸入人員<\/option>/);assert.doesNotMatch(view,/report-autocomplete/);assert.match(script,/RequestVerificationToken/);assert.match(script,/input-users\?endDate=/);assert.match(script,/mounted\(\)\{this\.loadUsers\(\)\}/);assert.match(script,/pageNumber:this\.currentPage/);assert.match(script,/currentPage=1/);assert.match(script,/\/reports\/c7\/preview/);assert.match(app,/C7:\s*window\.ReportComponents\.C7Report/);const window={};vm.runInNewContext(script,{window,document:{}});const c=window.ReportComponents.C7Report;const f=c.methods.initialForm.call({defaultStartDate:"2026-01-01",defaultEndDate:"2026-01-02"});assert.equal(f.startTime,"0000");assert.equal(f.endTime,"2359");assert.equal(f.chargeKind,0);assert.equal(f.inputUserId,"");const payload=c.methods.payload.call({form:f,currentPage:1,pageSize:10});assert.equal(payload.inputUserId,"");console.log("c7 report UI tests passed");
